import { FileText, Receipt, Calculator, Users, BookOpen, FileSignature, ScrollText, ClipboardList } from "lucide-vue-next";

export const serviceIcons = {
  kundreskontra: FileText,
  leverantorsreskontra: Receipt,
  bokforing: Calculator,
  loner: Users,
  bokslut: BookOpen,
  arsredovisning: FileSignature,
  deklaration: ScrollText,
  pappersarbete: ClipboardList
};